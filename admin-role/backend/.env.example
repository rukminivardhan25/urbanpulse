# Admin Backend Environment Variables

# Server Port (Different from user backend - user backend uses 3000)
PORT=3001

# Fast2SMS API Key (Can use same as user backend or separate)
FAST2SMS_API_KEY=your_fast2sms_api_key_here

# JWT Secret for Admin Tokens (Should be different from user backend for security)
ADMIN_JWT_SECRET=your_admin_jwt_secret_key_here_change_this_to_random_string

# MongoDB Connection (Same database as user backend, but different collection)
# Copy the MONGODB_URI from backend/.env
MONGODB_URI=mongodb://localhost:27017/urbanpulse

# OTP Configuration
OTP_EXPIRY_MINUTES=5
OTP_RESEND_COOLDOWN_SECONDS=60

# Environment
NODE_ENV=development
